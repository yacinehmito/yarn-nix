# This is the configuration.nix that you should put in /etc/nixos.
# Replace device-name by the name of your device and /path/to/yarn with
# the path to the yarn repository folder
(import /path/to/yarn).device-name
